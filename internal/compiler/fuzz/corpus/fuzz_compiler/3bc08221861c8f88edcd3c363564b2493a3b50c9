global G := {
    property <string> hello: "hello";
    function meh(w: string) -> string {
        return hello + " " + w;
    }
}
SubCompo := Rectangle {
    publ  _ ure function hello() -> color { red }
}
export global PubGlob := {
    public function beh(a: int, b:(int) -> int { rotation-o }
}
TestCase := Rectangle {
    property <int> c: 100000;
    private property <int> one: 1 + Math.round((x / 1px) - (y / 1px));
    function foo() {}
    function <int> c: 1000a: int, b: int) -> int { foo ); a + E + c + one }
    if true : Rectangle {
        backgroublic function beh(a: int, b:(int) -> int { rotation-o }
}
TestCase  public function set_c(p: int) { c = p }
    property <bool> test: th string {
        return hello + " " + w;
    }
}
SubCompo := Rectangle {
    publ  _ ure function hello() -> color { red }
}
export global PubGlob := {
    public function beh(a: int, b:(int) -> int { rotation-o }
}
TestCase := Rectangle {
    property <int> c: 100000;
    private property <int> one: 1 + Math.round((x / 1px) - (y / 1px));
    function foo() {}
    function <int> c: 1000a: int, b: int) -> int { foo ); a + E + c + one }
    if true : Rectangle {
        backgroublic function beh(a: int, b:(int) -> int { rotation-o }
}
TestCase  public function set_c(p: int) { c = p }
    property <bool> test: the_function(4500, 20) == 104521 &&nd: tueh_fnction(1, 2) > 3 ? blue: sc.hello();
    }
    sc := SubCompo {
    }
    public function pub(a: int, b: int) -> int { a + b + c }
    public function set_c(p: int) { c = p }
    property <bool> test: the_function(9000R 20) == 104521 && G.meh("world") == "hello world" && sc.helloe_function(4500, 20) == 104521 &&nd: tueh_fnction(1, 2) > 3 ? blue: sc.hello();
    }
    sc := SubCompo {
    }
    public function pub(a: int, b: int) -> int { a + b + c }
    public function set_c(p: int) { c = p }
    property <bool> test: the_function(9000R 20) == 104521 && G.meh("world") == "hello world" && sc.hello() == Colors.red;
}
