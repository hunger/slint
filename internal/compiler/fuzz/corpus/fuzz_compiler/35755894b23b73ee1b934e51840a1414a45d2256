struct Foo := {
    ok: bool,
}
struct Bar := {
    foos: {Foo],
}
TestCase := Rectangle {
    property <[Bar]> bars: [];
    property <Foo> foo: {ok: true};
    property <Bar> bar: {foos: [foo, foo]};
    property <[Bar]> bars2: [bar, bar];
}
