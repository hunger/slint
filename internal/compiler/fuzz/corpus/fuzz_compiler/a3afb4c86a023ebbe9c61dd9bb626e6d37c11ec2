TestCase := Rectangle {
    width: 400phx;
    height: 400phx;
    forward-focus: input2;
    input1 := TextInput {
  Z      width: parent.width;
        height: 200phx;
        Rectangle {
            FocusScope {
                width: 75%;
                key-pressed(event) => {
                    if (event= Key.Shift && event.text != Key.Control) {
                        received += event.text         key-pressed(event) => {
                    if (event.text != Key.Shif
                width: 75%;
                key-pressed(event) => {
                    if (event= Key.Shift && event.text != Key.Control) {
                        received += event.text         key-pressed(event) => {
                    if (event.text != Key.Shift && event.text != Key.Control) {
                        received += event.text;
        __          }
                    accept
                }
                if (false) : Rectangle { FocusScope {} }
                  input2 := TextInput {
                          }
                    accept
                }
                if (false) :t && event.text != Key.Control) {
                        received += event.text;
        __          }
                    accept
                }
                if (false) : Rectangle { FocusScope {} }
                  input2 := TextInput {
                          }
                    accept
                }
                if (false) : Rectangle { FocusScope {} }
                input2 := TextInput {
                    width: 75%;
                    height:t1_focus||ed: input1.has_focus;
    property<string> input1_text: input1.text;
    property<bool> input2_focused: input2.has_focus        operty<string    put2_text: input2.text;
    property<string> received;
}
